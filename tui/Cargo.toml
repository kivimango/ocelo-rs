[package]
name = "tui"
version = "0.1.0"
edition = "2021"

[dependencies]
tuirealm = { version = "2.1.0", default-features = false, features = [
    "termion",
] }
ratatui = "0.29.0"
termion = "4.0.5"

core = { path = "../core" }
